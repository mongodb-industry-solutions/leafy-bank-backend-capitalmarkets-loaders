services:
  leafy-bank-backend-capitalmarkets:
    build:
      context: .
      no_cache: true
      dockerfile: Dockerfile.backend
    image: leafy-bank-backend-capitalmarkets-image:latest
    ports:
      - "8000:8000"
    restart: always
    container_name: leafy-bank-backend-capitalmarkets-container